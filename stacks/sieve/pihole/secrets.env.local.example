# Auto-generated by ../generate-secrets.sh — this .example file is
# documentation only; the real secrets.env.local is gitignored.
#
# PIHOLE_WEBPASSWORD is currently UNUSED (decision 2026-08-29): Pi-hole's
# own web password is disabled in docker-compose.yml
# (FTLCONF_webserver_api_password: "") in favor of Authelia's forward-auth
# as the sole gate, now that the admin UI is routed through Traefik. Still
# generated here, gitignored, harmless — kept around in case that decision
# gets revisited (see runbook backlog).
PIHOLE_WEBPASSWORD=

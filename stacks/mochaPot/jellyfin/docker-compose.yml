# MOVED 2026-09-05 -- Jellyfin no longer lives on mochaPot. See
# stacks/roastery/jellyfin/docker-compose.yml for the real file, and this
# repo's runbook.md / initiation.txt (Section 0.3's Container reallocation
# paragraph) for the full reasoning: Quick Sync struggled decoding video
# in an earlier real deployment (user's own report, on percolator's
# same-generation iGPU), and roastery's RTX 3080 -- with GPU passthrough
# already confirmed working the same day -- is a materially stronger
# transcode path. Every other mochaPot app is unaffected; this was a
# single-app move, not a re-scoping of the node.
#
# Deliberately left as a non-functional stub rather than deleted --
# device-bridge sandbox tooling can't delete files in this repo. Run
# `git rm -r stacks/mochaPot/jellyfin` yourself to actually remove this
# directory once you've confirmed roastery's copy is what you want to
# keep. This file has no `services:` key on purpose, so `./compose.sh
# jellyfin up -d` fails loudly here instead of silently starting the old
# Quick-Sync config again.
